describe('The Search Bar component', () => {
    test.todo('properly renders');
    test.todo('takes in a input and immediately sends along an update to the provided handler function');
    test.todo('can delay sending an input until submitted manually if prompted');
});